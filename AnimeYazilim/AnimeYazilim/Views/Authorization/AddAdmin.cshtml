@model EntityLayer.Concrete.Admin
    @{
        ViewBag.Title = "AddAdmin";
        Layout = "~/Views/Shared/_AdminLayout.cshtml";
    }

    <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
    <script src="~/Scripts/jquery-3.4.1.js"></script>


    <h1>Yeni Admin Ekleme Sayfası</h1>
    <br />
    <form action="/Authorization/AddAdmin" method="post">
        <div class="form-group">
            <input name="Status" value="True" hidden="hidden" />
            <label class="control-label">Admin Adı</label>
            <input name="AdminUserName" type="text" autocomplete="off" class="form-control" />
        </div>
        <div class="form-group">
            <label class="control-label">Şifre</label>
            <input name="AdminPassword" type="text" autocomplete="off" class="form-control" />
        </div>
        <div class="form-group">
            <label class="control-label">Rol</label>
            <select class="form-control" name="AdminRole">
                <option id="A">A</option>
                <option id="B">B</option>
                <option id="C">C</option>
            </select>
        </div>
        <button class="btn btn-primary" type="submit">Kaydet</button>
    </form>


@*@model EntityLayer.Dextra.LoginDto
@{ ViewBag.Title = "AddAdmin";
                Layout = "~/Views/Shared/_AdminLayout.cshtml"; }

<h2>Yeni Admin Ekleme Sayfası</h2>
<br />
@using (Html.BeginForm("AddAdmin", "Authorization", FormMethod.Post))
{
<div class="form-group">
    @Html.Label("Admin Mail")
    @Html.TextBoxFor(c => c.AdminUserName, new { @class = "form-control" })
    @Html.ValidationMessageFor(c => c.AdminUserName)
    <br />
    @Html.Label("Şifreyi yazınız")
    @Html.TextBoxFor(c => c.AdminPassword, new { @class = "form-control" })
    @Html.ValidationMessageFor(c => c.AdminPassword)
    <br />
    @Html.Label("Adminin rolünü yazınız")
    @Html.DropDownListFor(c => c.RoleId, (List<SelectListItem>)ViewBag.valueadmin, new { @class = "form-control" })
    <br />
    <button class="btn btn-info" id="buton">Admin Ekle</button>
</div>}*@
